/**
 * 4-way Or:
 * out = (in[0] or in[1] or in[2] or in[3])
 */

CHIP Or4Way {
    IN in[4];
    OUT out;

    PARTS:
    Or(a=in[0], b=in[1], out=or-0-1);
    Or(a=in[2], b=in[3], out=or-2-3);
    Or(a=or-0-1, b=or-2-3, out=out);
}
